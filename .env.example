# Kopieren nach .env und Passwort setzen.
# Compose liest diese Werte fuer Interpolation und gibt sie an den Postgres-Container.
POSTGRES_USER=invoice_user
POSTGRES_PASSWORD=change-me
POSTGRES_DB=invoice_db
