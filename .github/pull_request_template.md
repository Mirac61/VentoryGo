## Was und warum

<!-- Eine bis drei Zeilen. Das Warum ist wichtiger als das Was -- den Diff sieht man. -->

Closes #

## Getestet

<!-- Wie geprüft? Neue Tests, manueller Aufruf, beides? -->

- [ ] `go test ./...` läuft durch
- [ ] `gofmt -l .` und `go vet ./...` sauber
- [ ] Frontend: `npm run lint` und `npm run build` sauber

## Auswirkung

- [ ] Breaking Change für die API
- [ ] Neue Migration dabei
- [ ] Frontend muss nachziehen
- [ ] Neue Environment-Variable (in `.env.example` ergänzt)
